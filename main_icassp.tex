% =====================================================================
%  ICASSP submission (5 pages: 4 content + 1 references-only).
%  v2 -- restructured around the prior-correction result. The v1
%  manuscript is preserved at main_icassp_v1_backup.tex and the reasoning
%  behind the restructure is in ICASSP_REVIEW_AND_PLAN.md.
% =====================================================================
\documentclass[conference]{IEEEtran}

\usepackage{cite}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage[table]{xcolor}
\usepackage{textcomp}
\setlength{\textfloatsep}{6pt plus 2pt minus 2pt}
\setlength{\intextsep}{6pt plus 2pt minus 2pt}
\setlength{\abovecaptionskip}{3pt}
\setlength{\belowcaptionskip}{2pt}
\usepackage{algorithm}
\usepackage{algpseudocode}

% Denser float packing: keeps all content inside the 4-page limit.
\renewcommand{\topfraction}{0.95}
\renewcommand{\bottomfraction}{0.7}
\renewcommand{\textfraction}{0.04}
\renewcommand{\floatpagefraction}{0.85}
\setcounter{topnumber}{3}
\setcounter{totalnumber}{4}

\newcommand{\safefig}[2]{%
  \IfFileExists{#2}{\includegraphics[width=#1]{#2}}{%
    \fbox{\parbox[c][2.2cm][c]{\dimexpr#1-2\fboxsep-2\fboxrule\relax}{%
      \centering\ttfamily\detokenize{#2}\\[3pt]\normalfont(upload this figure)}}}%
}
% Visible marker for numbers still being produced by the running GPU queue.
\newcommand{\PENDING}[1]{{\color{red}\textbf{[#1]}}}

\begin{document}

\title{Ranking Transfers, Thresholds Don't:\\
Prior-Corrected Test-Time Adaptation for Audio Deepfake Detection}

\author{\IEEEauthorblockN{Roham Izadidoost}
\IEEEauthorblockA{Manipal University Jaipur \\
izadidoostroham@gmail.com}
\and
\IEEEauthorblockN{Dr.~Sumit Srivastava}
\IEEEauthorblockA{Manipal University Jaipur \\
sumit.srivastava@jaipur.manipal.edu}
\and
\IEEEauthorblockN{Shweta Sharma}
\IEEEauthorblockA{Manipal University Jaipur \\
Shweta.sharma1@jaipur.manipal.edu}}

\maketitle

\begin{abstract}
Audio deepfake detectors generalise poorly across corpora despite near-perfect
in-domain accuracy. We show the failure is one of \emph{operating point}: a
fine-tuned self-supervised detector's \emph{ranking} (ROC-AUC) largely transfers
cross-corpus while its decision \emph{threshold} does not, so unlabeled target
audio can repair the operating point at test time. We then show that
confident-quantile test-time adaptation (TTA) can be sound only while its tail
budget satisfies $q\le\min(\pi,1{-}\pi)$: a counting bound caps pseudo-label
purity at $\min(1,\pi_{\mathrm{real}}/q)$ \emph{for any detector}. That holds where TTA is measured and
essentially nowhere else --- ASVspoof2021-DF is $97.2\%$ spoof --- so at the
published $q{=}0.3$ no model can exceed $9.3\%$ purity there. Resampling
In-the-Wild's prevalence, holding audio and weights fixed, reproduces the failure
on demand: accuracy tracks the bound across a $3{\times}3$ grid (Spearman
$+0.966$). Empirically, on the official DF eval ($400{,}435$ trials) that
configuration destroys the operating point of three released state-of-the-art checkpoints,
driving accuracy from $98.8$--$99.1\%$ to $62$--$65\%$ across nine paired cells;
three of four published TTA baselines degrade all three as well --- ETA reaching
$31$ and $37\%$ EER against a source $3.8$ and $4.6$ --- each through its own
version of the assumption. Correcting the budget with a label-free prior estimate
($\hat\pi{=}0.9715$ vs.\ a true $0.9708$) restores all of them and beats the
symmetric budget on $9/9$ cells ($p{=}0.004$); both EER and AUC move, so no
re-thresholding rule accounts for it. Its boundary we measure rather than assume: the prior is not identifiable
from a model's own outputs, so the estimate degrades exactly as the operating
point does ($r{=}0.997$ over ten cells), and a wrong prior is worse than none.
Since $q$ is ours to choose and $\pi$ is not, shrinking $q$ satisfies the
condition without estimating anything.
\end{abstract}

\begin{IEEEkeywords}
audio deepfake detection, anti-spoofing, test-time adaptation, label shift,
cross-corpus generalisation, EER.
\end{IEEEkeywords}

\section{Introduction}
Self-supervised (SSL) front-ends such as wav2vec\,2.0 and XLS-R, paired with
lightweight back-ends, reach very low in-domain error on standard anti-spoofing
benchmarks~\cite{wav2vec2aug,xlsrsls,aasist}. Deployed on audio from a
different source---new microphones, codecs, languages, or generators---accuracy
drops steeply, a well-documented generalisation gap~\cite{asdg,crossdomain,itw}.

We start from a diagnostic measurement: a fine-tuned XLS-R detector evaluated on
an unseen corpus keeps a high \textbf{ROC-AUC while its accuracy at a fixed
threshold collapses}. It still \emph{ranks} clips correctly; its operating point
is mis-calibrated. Because ranking transfers, the most-confident predictions on
unlabeled target audio are trustworthy enough to adapt on at test time.

That observation is the starting point of a family of methods, not a method.
The step every member of the family takes---label the extreme quantiles of the
target scores and self-train on them---carries an assumption that is invisible
on the benchmarks the field uses and fatal on the pools it deploys to: that the
target pool is roughly class-balanced. Take the top and bottom $30\%$ of a
$97\%$-spoof pool and the ``confidently bona fide'' bucket is mechanically made
of low-scoring \emph{spoof}. Self-training then teaches the detector that a
great deal of spoof is genuine. This is not hypothetical: it is what happens to all three released
state-of-the-art checkpoints we test on the official ASVspoof2021-DF protocol
(Table~\ref{tab:protoa}), and to three of four modern TTA baselines.

\textbf{Contributions.} (i) The ranking--calibration asymmetry as the mechanism
of cross-corpus failure, over $369$ third-party cells and tested directly: a
proxy $\mathcal{A}$-distance \emph{rises} after adaptation, so the effect is
operating-point repair, not learned invariance. (ii) An exact validity condition
for the confident-quantile rule, $q\le\min(\pi,1{-}\pi)$, from a counting bound on
pseudo-label purity; it predicts every result here, including why three of four
modern TTA baselines destroy three released SOTA checkpoints at the standard
benchmark's own prevalence, and it is confirmed by intervention. (iii) A
label-free correction that removes the damage on every DF checkpoint, together
with an identifiability argument bounding when it can be trusted --- and the
demonstration, via an oracle prior, that what fails is the estimate and not the
correction. (iv) Two label-free monitors that separate damaging adaptations from
safe ones, one of which recovers $13.9$ EER points from a Tent collapse
out-of-sample.

\textbf{Related work.} SSL front-ends with AASIST or attentive back-ends dominate
recent benchmarks~\cite{wav2vec2aug,xlsrsls,aasist,rawnet2}. Train-time domain
generalisation aligns source domains~\cite{asdg} or runs gradient reversal over
source and target jointly~\cite{dann}; both act before deployment. In TTA,
entropy minimisation (Tent)~\cite{tent} is the reference recipe, and its
instability prompted reliable-sample filtering (ETA/EATA)~\cite{eata},
sharpness-aware entropy with recovery (SAR)~\cite{sar}, and information
maximisation with self-labelling (SHOT)~\cite{shot} --- we compare against all
four. Batch-statistics recalibration~\cite{adabn} is a no-op for LayerNorm. The
prior estimator is black-box shift estimation~\cite{bbse}; the proxy
$\mathcal{A}$-distance is from~\cite{bendavid}.

\section{Method}
\label{sec:method}
Let $x$ be a waveform with label $y\in\{0,1\}$ (fake$=1$) and let $f_\theta$
produce $s_\theta(x)=P_\theta(y{=}1\mid x)$. We are given a source model, its
labelled training corpus $\mathcal{S}$, and an \emph{unlabelled} target pool
$\mathcal{T}$. No target label is read at any point.

\textbf{What breaks under corpus shift.} Ranking quality
$\mathrm{AUC}=\Pr[s_\theta(x^+){>}s_\theta(x^-)]$ is threshold-free; accuracy at
a fixed $\tau$ is not. Empirically AUC degrades far less than accuracy at
$\tau{=}0.5$ under source$\to$target shift: across $369$ third-party evaluation
cells the accuracy a model's ranking supports but its shipped threshold does not
deliver averages $7.29$ points. The deployable object is therefore the score
\emph{order}, and the broken object is the operating point. Everything below follows from taking that literally.

\textbf{(a) The target prior, label-free.} An operating point is a statement
about a prior, so we estimate the prior rather than assume it. Black-box shift
estimation~\cite{bbse} gives $\hat p = M^{-\top}q$ with
$M[y,\hat y]=P_{\mathcal S}(\hat y\mid y)$ the source confusion matrix and $q$ the
target prediction histogram. Estimators split into those reading the
\emph{thresholded prediction rate} (BBSE, EM~\cite{sld}) and those reading the
score \emph{histogram} (a two-component mixture); the first inherits whatever
calibration error the target induces, and Secs.~\ref{sec:results}
and~\ref{sec:lim} show neither is reliable where it is needed.

\textbf{(b) The tail budget, and when it is valid.} The standard rule takes the
bottom $q$ and top $q$ of the target scores as pseudo-real and pseudo-fake. Its
validity has a characterisation that needs no model at all. A bucket of $q_cN$
clips can contain at most the pool's $\pi_cN$ class-$c$ clips, so
\emph{for any detector whatsoever}
\begin{equation}
\mathrm{purity}_c\;\le\;\min\!\big(1,\ \pi_c/q_c\big).
\label{eq:purity}
\end{equation}
This is counting, not an approximation; what is empirical is its
\emph{tightness}, which is where the ranking precondition enters --- across all
seven (checkpoint, corpus) cells here the bound is never violated and is attained
to within $0.025$, the slack tracking AUC ($0.000$ at AUC $.985$, $0.025$ at
$.917$). Two consequences follow, and they are the whole story.

The \emph{symmetric} rule sets $q_c{=}q$ and is therefore sound only while
\begin{equation}
q\;\le\;\min(\pi,\,1-\pi).
\label{eq:valid}
\end{equation}
In-the-Wild satisfies this at $q{=}0.3$ ($\min{=}.372$) and the recipe repairs
checkpoints there; DF does not ($\min{=}.028$), where no detector can exceed
$9.3\%$ pseudo-real purity --- we measure $8.6\%$ --- and the recipe destroys
them. The damage is one-sided: DF's pseudo-\emph{fake} tail is $100\%$ pure at
every $q$.

The \emph{prior-split} rule, $q_c{=}2q\hat\pi_c$, restores
Eq.~\eqref{eq:purity} for \emph{any} $q\le\tfrac12$ given a correct prior, and is
identical to the symmetric rule at $\hat\pi{=}0.5$. With an estimate it needs
\begin{equation}
\hat\pi_c/\pi_c\;\le\;1/(2q)\quad\text{for both classes,}
\label{eq:tol}
\end{equation}
i.e.\ at $q{=}0.3$ the estimate may overstate either class by at most $1.67\times$.
This is why it works on DF ($\hat\pi_{\mathrm{real}}/\pi_{\mathrm{real}}{=}1.03$)
and fails on In-the-Wild s42, where underestimating $\pi_{\mathrm{real}}$ inflates
the \emph{fake} budget to $0.587$ against $\pi_{\mathrm{fake}}{=}0.372$
($2.63\times$, bound $0.633$) and simply moves the impurity to the other tail.
Across the seven adapted arms we ran, the worst-tail bound is $1.000$ for the
five that came out intact ($82$--$99\%$ accuracy) and $0.093$ and $0.633$ for the
two that collapsed ($62.96$, $47.99$). Since $q$ is ours to choose and $\pi$ is
not (Sec.~\ref{sec:lim}), a third option is to shrink $q$ until
Eq.~\eqref{eq:valid} holds for the most skewed prior anticipated --- at a
supervision cost the prior-split budget avoids ($q{=}0.02$ labels $800$ of
$20{,}000$ clips against $12{,}000$). A fixed confidence threshold is a fourth,
prior-free but calibration-dependent. We report all four.

\textbf{(c) Objective.} With pseudo-labels $\tilde y$ from (b) on the confident
set $\mathcal{C}$ and a stochastic channel perturbation $a(\cdot)$ (per-clip
gain $g\sim\mathcal U(0.7,1.3)$ plus Gaussian noise $\sigma{=}0.005$),
\begin{equation}
\min_{\{\gamma,\beta\},\phi}\
\underbrace{\textstyle\sum_{x\in\mathcal C}\!\mathrm{CE}\big(f_\theta(x),\tilde y\big)}_{\mathcal L_{\mathrm{ST}}}
+\ \lambda\,\underbrace{\big\|f_\theta(a(x))-\mathrm{sg}[f_\theta(x)]\big\|^2}_{\mathcal L_{\mathrm{cons}}},
\end{equation}
optimising only the LayerNorm affine parameters $\{\gamma,\beta\}$ of the
fine-tuned blocks and the head $\phi$ ($16{,}706$ parameters), $\lambda{=}0.3$.
Restricting the trainable set and using only prior-corrected confident tails is
what prevents the degenerate collapse entropy minimisation is prone to. The
consistency term is inherited from the published recipe; an ablation finds it
contributes nothing here ($3.98$ vs $4.02$ EER), so the budget is doing the work.

\textbf{(d) Two label-free guards.} Adaptation can damage the model it is
given, so a deployer needs to decide --- without labels --- whether to keep the
adapted weights. Since the source ranking is the asset being exploited, we
compare the adapted scores' order against the frozen source model's,
$\rho=\mathrm{Spearman}\big(s_{\theta}(\mathcal T),s_{\theta_0}(\mathcal T)\big)$,
and separately track the drift of the predicted positive rate from
$\hat\pi$. We use these \emph{post hoc}, as a keep/abort decision on a completed
adaptation; a per-step stopping rule is the natural extension and we do not
claim it here.

\section{Experimental Setup}
\textbf{Protocol A.} The official ASVspoof2021-DF eval partition against the
official CM keys; $400{,}435$ of $611{,}829$ trials are held locally, the missing
part uniform across codec, label and phase (verified $75$--$25$ in every
stratum), true spoof rate $97.22\%$. Published pooled EERs here run from $1.95$
(Wav2DF-TSL) and $2.87$ (SSL$+$SLS) through $8.54$ (Wav2Vec2-AASIST) to $15.64$
(challenge top-1); our own source model reaches only $26.33$, so we run the
method on \emph{released} checkpoints instead: DeepFense
XLS-R\,300M\,+\,AASIST trained on ASVspoof2019-LA~\cite{deepfensehf}, three
independent training seeds. Adaptation uses an unlabelled $20{,}000$-clip
subsample; we report the full official pooled EER, an inductive number over
trials \emph{not} in that subsample, and two further seeds on a fixed $50{,}000$
subset, giving nine paired cells.

\textbf{Protocol B.} Leave-one-corpus-out over ASVspoof2019-LA~\cite{asvspoof19},
D2 (LibriSpeech vs.\ eight open TTS systems), In-the-Wild~\cite{itw} and the
Arabic set~\cite{arad}; each source pool merges the other three with a
38-language MLAAD subset~\cite{mlaad}, held-out target audio never appears in
any source pool, and six MLAAD languages are held out of every pool. Every
method runs on the same twelve folds (four targets, three seeds).

\textbf{Protocol C.} All $31{,}779$ In-the-Wild clips at their native $37.2\%$
fake rate, skewed the \emph{opposite} way to DF.

\textbf{Comparison points.} Source-only; the median-threshold rule; Tent, SHOT,
ETA/EATA, SAR; AdaBN~\cite{adabn}; DANN~\cite{dann} (which sees the held-out
target's unlabeled audio every step, more privileged access than ours); an
ASDG-style baseline~\cite{asdg}; and a target-supervised oracle. Every TTA arm
adapts the same $16{,}706$ parameters with the same lr, batch and pass count;
none was tuned separately, and none could be, since per-target tuning needs the
labels the setting assumes absent. Two published constants are degenerate at
$C{=}2$ and were re-derived: EATA's redundancy filter ($|\cos|{<}0.05$ is
unreachable until the running mean hits a simplex vertex, after which only
minority-predicted samples pass) and SAR's recovery trigger ($e_0{=}0.2$ sits
inside the $C{=}2$ reliable band and freezes the model; we keep its relative
position, $e_0{=}(0.2/\ln 1000)\ln C$).

\textbf{Implementation.} 16\,kHz audio, top-4-layer XLS-R fine-tuning for our own
source models (Adam, lr\,$2{\times}10^{-4}$, batch 32, 8 epochs). Adaptation:
lr\,$1{\times}10^{-4}$, $E{=}4$ passes, batch $32$ (third-party crops matched to
each checkpoint's own training length). Code and manifests:
\texttt{[URL withheld]}.

\section{Results}
\label{sec:results}

\begin{table}[t]
\centering
\caption{Official ASVspoof2021-DF eval, $400{,}435$ trials, $97.22\%$ spoof,
released XLS-R+AASIST checkpoint seed~2~\cite{deepfensehf}; every arm was also
run on seeds 42 and 240 (text). All adaptation is label-free. Deficit $\delta=(100{-}\mathrm{EER})-\mathrm{acc}@0.5$
is the accuracy the ranking supports but the threshold does not deliver. $\rho$
and the prediction-rate gap are the label-free monitors of
Sec.~\ref{sec:method}(d) --- computable at deployment, unlike every other
column.}
\label{tab:protoa}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{@{}lcccccc@{}}
\toprule
& EER\,\% & AUC & acc@$0.5$ & deficit & $\rho$ & gap \\
\midrule
Source (as released) & 4.51 & .9923 & 98.87 & $-3.38$ & 1.000 & .000 \\
\;+ median-threshold rule & 4.51$^\dagger$ & .9923$^\dagger$ & 59.06 & --- & --- & --- \\
\;+ ETA~\cite{eata} & 7.44$^\ddagger$ & .9288 & 98.93 & $-6.37$ & 0.312 & .007 \\
\;+ Tent~\cite{tent} & 4.06$^\ddagger$ & .9765 & 98.52 & $-2.58$ & 0.438 & .013 \\
\;+ SHOT~\cite{shot} & 6.38 & .9725 & 77.64 & $+15.99$ & 0.767 & .225 \\
\;+ TTA, fixed confidence $0.95$ & 4.30 & .9924 & 99.02 & $-3.32$ & 0.827 & .005 \\
\;+ TTA, symmetric $q{=}0.3$ & 5.84 & .9846 & 62.96 & $+31.20$ & 0.816 & \textbf{.372} \\
\rowcolor{green!12}\;+ TTA, \textbf{prior-corrected} & \textbf{4.02} & \textbf{.9931} & \textbf{98.95} & $-2.97$ & 0.867 & .007 \\
\;\;\;\;(disjoint/inductive half) & 4.03 & .9931 & 98.95 & & & \\
\;\;\;\;(no consistency term) & 3.98 & .9936 & 98.90 & $-2.88$ & 0.884 & .003 \\
\;+ SAR~\cite{sar} & 4.47$^\S$ & .9923 & 98.88 & $-3.35$ & 0.970 & .000 \\
\bottomrule
\end{tabular}}
\\[2pt]
\footnotesize $^\dagger$ A threshold cannot change EER or AUC; both come from the
score ROC. The median rule is a control for the \emph{accuracy} claim and cannot
produce any EER result in this paper. $^\ddagger$ EER over a pool that is more
than $93\%$ a single tied score (Tent saturates $98.07\%$ of scores to exactly
$1.0$, ETA $97.71\%$); read AUC. $^\S$ SAR neither damages nor adapts: $308$
recovery resets in $2{,}500$ steps, AUC unchanged to four decimals.
\end{table}

\subsection{The class prior is the hidden variable}
Table~\ref{tab:protoa} is the main result, and
Eqs.~\eqref{eq:purity}--\eqref{eq:valid} anticipate it. Applied unmodified to a
released checkpoint already better than the published Wav2Vec2-AASIST baseline
here, the symmetric-quantile configuration \emph{damages} it: accuracy
$98.87\!\to\!62.96\%$, EER $4.51\!\to\!5.84\%$, manufacturing a $31$-point deficit
where there was none.

The published baselines are no better placed, and each fails through its own
version of the assumption. SHOT minimises $\mathbb{E}[H(p)]-H(\mathbb{E}[p])$,
whose diversity term is maximised when the mean prediction is \emph{uniform} ---
at $C{=}2$ a hard prior of $\tfrac12$; the regulariser introduced to \emph{cure}
Tent-style collapse encodes the assumption more literally than Tent does. ETA
fails more instructively: its reliability filter, meant to discard the
high-entropy samples that destabilise entropy minimisation, admitted $99.8\%$ of
them, so ETA reduced to Tent and landed worse. An entropy-keyed filter separates
confident from uncertain, not \emph{confident-and-right} from
\emph{confident-and-wrong}, and mis-calibration manufactures exactly the latter.
SAR is the exception that clarifies the rule: the only prior method that does
not degrade the checkpoint, and it manages that by declining to move it ($308$
recovery resets in $2{,}500$ steps, AUC unchanged to four decimals). Across all
three checkpoints ETA reaches $31.0$ and $37.5\%$ EER (source $3.8$, $4.6$), Tent
$17.6$ and $11.2$, SHOT costs $17$--$26$ accuracy points on every one, and SAR is
inert on every one. So the claim is not that every published method fails here,
but that \emph{three of five prior configurations degrade a state-of-the-art
detector at the standard benchmark's own prevalence, the fourth avoids damage
only by declining to adapt, and none improves it.}

The prior-split budget reverses this: BBSE returns $\hat\pi{=}0.9715$ against a
true $0.9708$, comfortably inside Eq.~\eqref{eq:tol}, and EER improves to
$4.02\%$. The change between the two arms is one scalar. Over three
independently trained checkpoints (EER\,\% / accuracy\,\%):

\begin{center}\footnotesize
\begin{tabular}{@{}lccc@{}}
\toprule
 & seed 2 & seed 42 & seed 240 \\
\midrule
source & 4.51 / 98.87 & 4.56 / 98.78 & 3.82 / 99.11 \\
\;+ symmetric $q$ & 5.84 / \textbf{62.96} & 4.08 / \textbf{61.80} & 4.66 / \textbf{65.24} \\
\;+ prior-corrected & \textbf{4.02} / 98.95 & \textbf{3.29} / 98.98 & \textbf{3.80} / 98.74 \\
\bottomrule
\end{tabular}
\end{center}

\noindent Over the nine paired cells (seed-to-seed EER std $\le0.14$),
prior-correction beats
the symmetric budget on \textbf{$9/9$} ($+1.23$ EER, Wilcoxon $p{=}0.004$) and
source-only on $8/9$ ($+0.48$, $p{=}0.027$), while the symmetric budget is worse
than source-only on $6/9$. Accuracy separates them with no overlap at all:
$98.8$--$99.1\%$ (source), $61.6$--$65.2$ (symmetric), $98.7$--$99.0$
(prior-corrected). What does \emph{not} replicate is the symmetric budget's
effect on \emph{ranking} (EER worse on two checkpoints, better on one), as
Eq.~\eqref{eq:purity} implies: a mis-specified budget corrupts the pseudo-labels
and hence the operating point, while the ranking is inherited.

\subsection{The failure, and where the recipe already works}
At In-the-Wild's native prior ($0.372$) Eq.~\eqref{eq:valid} \emph{holds}, and
the same recipe repairs rather than destroys. DeepFense s42 --- the checkpoint
that scores $4.56\%$ on DF --- arrives here ranking at AUC $0.917$ while correct
on only $39.1\%$ of clips, fewer than a majority-class predictor. Self-training
repairs exactly that: over four released checkpoints it drives the deficit from
$16.7$, $22.6$ and $44.4$ to $0.1$, $2.9$ and $5.0$ (and costs a little on the
fourth, which arrives well-calibrated at $2.1$ and has nothing to repair). On
s42 it also raises \textbf{AUC $.917\!\to\!.947$} --- which no threshold can do
--- with the disjoint half slightly better ($12.27$/$.949$), so the ranking gain
is not memorisation.

\subsection{Testing the bound by intervention}
Eqs.~\eqref{eq:purity}--\eqref{eq:valid} are claims about the budget, so we move
the budget. Sweeping $q$ on DF s2 with everything else fixed gives accuracy
$98.96$, $95.68$, $88.15$, $62.96$ at $q{=}0.02,0.05,0.10,0.30$ against bounds
$1.000$, $0.556$, $0.278$, $0.093$ --- monotone in the bound (Spearman
$+1.000$). At $q{=}0.02$,
inside Eq.~\eqref{eq:valid} for this pool, the \emph{unmodified symmetric rule}
is safe and improves on source ($4.51\!\to\!4.19$) --- no prior estimate anywhere.
It remains weaker than the prior-split budget ($4.02$) for the reason
Eq.~\eqref{eq:purity} implies: shrinking $q$ fixes a minority-tail problem by
discarding the majority tail too ($800$ of $20{,}000$ clips labelled, against
$12{,}000$).

\textbf{Inducing the failure where the recipe works.} DF is one benchmark that
happens to be $97.2\%$ spoof, so we intervene on prevalence directly: resample
In-the-Wild --- where $q{=}0.3$ satisfies Eq.~\eqref{eq:valid} and repairs
checkpoints --- to a target $P(\mathrm{fake})$, holding audio and weights fixed.
Accuracy, bound in parentheses:

\begin{center}\footnotesize
\begin{tabular}{@{}lccc@{}}
\toprule
$P(\mathrm{fake})$ & $q{=}0.02$ & $q{=}0.10$ & $q{=}0.30$ \\
\midrule
$0.90$ & \textbf{97.8} (1.00) & 95.9 (1.00) & 73.3 (0.33) \\
$0.95$ & \textbf{98.6} (1.00) & 92.6 (0.50) & 68.5 (0.17) \\
$0.97$ & \textbf{98.6} (1.00) & 89.4 (0.30) & 65.4 (0.10) \\
\bottomrule
\end{tabular}
\end{center}

\noindent Accuracy tracks the bound (Spearman $+0.966$): $q{=}0.3$ collapses
monotonically, $q{=}0.10$ degrades from $0.95$ exactly where its bound does, and
$q{=}0.02$ is unharmed. The failure belongs to prevalence, not to DF.

\textbf{Is the correction wrong, or the estimate?} On the hardest cell
(In-the-Wild s42, $\delta{=}44.4$) we substitute the \emph{true} prior:

\begin{center}\footnotesize
\begin{tabular}{@{}lcccc@{}}
\toprule
prior used & none (sym.) & BBSE & mixture & \textbf{true} \\
\midrule
EER / accuracy & 12.58 / 82.4 & 14.51 / 48.0 & 14.95 / 55.0 & \textbf{11.59} / \textbf{91.3} \\
\bottomrule
\end{tabular}
\end{center}

\noindent With the true prior the prior-split budget is the best method in this
paper on its hardest cell; with either estimate it is worse than using no prior.
The mechanism is sound and the estimation is not --- exactly what
Sec.~\ref{sec:lim} proves must happen.

\textbf{Not a re-thresholding result.} Both EER and AUC move, and neither takes
a threshold as input: the same prior estimate used as a threshold leaves EER at
$4.51$ by construction, and as a budget gives $4.02$.

\subsection{Cross-corpus, balanced pools: where the advantage disappears}
Here Eq.~\eqref{eq:valid} holds and the prior correction is inert by
construction, so this arm measures the rest of the method. On twelve identical
folds (four leave-one-corpus-out targets, three seeds) we beat source-only
($18.39\!\to\!17.44$ pooled EER, $10/12$, $p{=}0.027$), Tent ($32.25$,
$p{=}0.012$), ETA ($29.54$, $p{=}0.003$), EATA ($31.34$, $p{=}0.016$) and SAR
($18.39$, $p{=}0.027$) --- but \textbf{not SHOT} ($17.76$ vs our $17.44$, $7/12$,
$p{=}0.47$), which is better than us on ASVspoof2019. That is the point rather
than an embarrassment: SHOT's diversity term is a hard uniform prior, free when
Eq.~\eqref{eq:valid} holds and destructive when it does not ($6.38\%$ EER and
$77.6\%$ accuracy on DF against our $4.02$ and $98.95$). The separation between
the two methods lives entirely in the regime the field does not test. Tent, ETA
and EATA are unstable even here, and SAR's pooled EER equals source-only to two
decimals --- inert on balanced pools exactly as it was on DF.

\subsection{Label-free guards}
Whether to keep an adapted model is itself decidable without labels. Over the
eight adapted DF arms, rank agreement $\rho$ and prediction-rate drift are
complementary --- $\rho$ catches ranking collapse (ETA $0.31$, Tent $0.44$), the
drift catches operating-point collapse ($0.37$, $0.39$, $0.23$) and is blind to
the former --- and requiring both separates the five harmful arms from the three
harmless ones exactly, for any $\tau\in[0.70,0.80]$. The plateau matters more
than the exactness: a $\rho$-only rule at $\tau{=}0.85$, fitted on seven arms,
looked equally exact and was falsified by the eighth, rejecting the study's best
result. Two thresholds on eight points is a diagnostic, not a rule, so we validate the
ranking guard as a \emph{stopping} rule out of sample: twelve folds ($E{=}32$),
threshold chosen leave-one-target-out. On Tent it recovers $13.9$ EER points ($32.25\!\to\!18.33$, oracle $17.24$) ---
back to source level from a catastrophic collapse --- while on our own method it
never fires and simply runs the budget out ($17.44\!\to\!16.58$, oracle $16.12$).
It prevents collapse and otherwise does not interfere, rather than selecting a
budget; churn and prior-drift are weaker still.

\subsection{Breadth, and the mechanism}
Over nine released checkpoints, five architectures and ten corpora ($369$
cells), the consistent effect is \emph{re-calibration}: $\delta$ falls
$7.29\!\to\!1.96$ ($248/369$, $p{=}2.2{\times}10^{-17}$), and where it lands barely
depends on where it started ($\mathrm{corr}{=}{+}0.17$). The mechanism is not
learned invariance: a proxy $\mathcal{A}$-distance and a linear domain-origin
probe both \emph{rise} after adaptation (Arabic $1.545\!\to\!1.606$), consistent
with DANN underperforming while optimising that quantity. Beyond any threshold
the gradient step adds separability where the ranking is strong: on ASVspoof2019
within-attack-family AUC rises on all six families ($30/30$, $p{=}10^{-9}$); on
the AUC-$0.71$ target it is null.

\section{Limitations}
\label{sec:lim}
\textbf{The prior is not identifiable, which bounds the correction.} A
label-free method sees only
$\Pr[\hat y{=}1]=\pi\,\mathrm{TPR}_{\mathcal T}+(1{-}\pi)\mathrm{FPR}_{\mathcal T}$:
one equation, three unknowns. BBSE closes it by assuming source error rates
carry over --- what covariate shift breaks --- so an extreme prior and an extreme
calibration failure look identical (DF/s2 predicts $97.4\%$ fake at a true
$0.972$, ITW/s42 $98.1\%$ at a true $0.372$). The estimate therefore degrades exactly as the operating point does --- over
ten cells and two corpora, $|\hat\pi-\pi|$ against the source deficit gives
Pearson $r{=}0.998$ --- and a badly wrong prior is worse than none (ITW/s42
$12.58\!\to\!14.51$ EER). Estimating the prior and detecting that the estimate is wrong are one problem;
two label-free detectors for it are falsified in our release. The gain also varies a great
deal ($1.27$ to $0.02$ EER), and a low-AUC start we cannot rescue.





{\small
\begin{thebibliography}{1}
\bibitem{wav2vec2aug} H.~Tak \emph{et al.}, ``Automatic speaker verification
spoofing and deepfake detection using wav2vec 2.0 and data augmentation,'' in
\emph{Proc.\ Odyssey}, 2022.
\bibitem{xlsrsls} Q.~Zhang, S.~Wen, and T.~Hu, ``Audio deepfake detection with
self-supervised XLS-R and SLS classifier,'' in \emph{Proc.\ ACM Multimedia},
2024.
\bibitem{aasist} J.~Jung \emph{et al.}, ``AASIST: Audio anti-spoofing using
integrated spectro-temporal graph attention networks,'' in \emph{ICASSP}, 2022.
\bibitem{rawnet2} H.~Tak \emph{et al.}, ``End-to-end anti-spoofing with RawNet2,''
in \emph{ICASSP}, 2021.
\bibitem{asdg} Y.~Xie \emph{et al.}, ``Domain generalization via aggregation and
separation for audio deepfake detection,'' \emph{IEEE TIFS}, 2024.
\bibitem{crossdomain} Y.~Li \emph{et al.}, ``Cross-domain audio deepfake
detection: dataset and analysis,'' in \emph{Proc.\ EMNLP}, 2024.
\bibitem{tent} D.~Wang \emph{et al.}, ``Tent: Fully test-time adaptation by
entropy minimization,'' in \emph{ICLR}, 2021.
\bibitem{eata} S.~Niu \emph{et al.}, ``Efficient test-time model adaptation
without forgetting,'' in \emph{ICML}, 2022.
\bibitem{sar} S.~Niu \emph{et al.}, ``Towards stable test-time adaptation in
dynamic wild world,'' in \emph{ICLR}, 2023.
\bibitem{shot} J.~Liang, D.~Hu, and J.~Feng, ``Do we really need to access the
source data? Source hypothesis transfer for unsupervised domain adaptation,''
in \emph{ICML}, 2020.
\bibitem{bbse} Z.~Lipton, Y.-X. Wang, and A.~Smola, ``Detecting and correcting
for label shift with black box predictors,'' in \emph{ICML}, 2018.
\bibitem{sld} M.~Saerens, P.~Latinne, and C.~Decaestecker, ``Adjusting the
outputs of a classifier to new a priori probabilities: A simple procedure,''
\emph{Neural Computation}, vol.\ 14, no.\ 1, pp.\ 21--41, 2002.
\bibitem{dann} Y.~Ganin and V.~Lempitsky, ``Unsupervised domain adaptation by
backpropagation,'' in \emph{Proc.\ ICML}, 2015.
\bibitem{adabn} Y.~Li \emph{et al.}, ``Revisiting batch normalization for
practical domain adaptation,'' \emph{arXiv:1603.04779}, 2016.
\bibitem{bendavid} S.~Ben-David \emph{et al.}, ``A theory of learning from
different domains,'' \emph{Machine Learning}, vol.\ 79, pp.\ 151--175, 2010.
\bibitem{ast} Y.~Gong, Y.-A. Chung, and J.~Glass, ``AST: Audio spectrogram
transformer,'' in \emph{Proc.\ Interspeech}, 2021.
\bibitem{itw} N.~M\"uller \emph{et al.}, ``Does audio deepfake detection
generalize?,'' in \emph{Proc.\ Interspeech}, 2022.
\bibitem{asvspoof19} X.~Wang \emph{et al.}, ``ASVspoof 2019: A large-scale
public database of synthesized, converted and replayed speech,'' \emph{Computer
Speech \& Language}, vol.\ 64, 101114, 2020.
\bibitem{mlaad} N.~M\"uller \emph{et al.}, ``MLAAD: The multi-language audio
anti-spoofing dataset,'' in \emph{Proc.\ IJCNN}, 2024.
\bibitem{arad} DeepFake-Audio-Rangers, ``Arabic Audio Deepfake dataset,''
HuggingFace datasets repository.
\bibitem{shiftyspeech} A.~Garg \emph{et al.}, ``ShiftySpeech: A large-scale
synthetic speech dataset with distribution shifts,'' \emph{arXiv:2502.05674},
2025. Checkpoint: \texttt{huggingface.co/ash56/ssl-aasist}.
\bibitem{deepfensehf} DeepFense, ``ASV19\_\-Wav2Vec2\_\-AASIST\_\-NoAug\_\-Seed\{2,42,240\},''
HuggingFace model repository. Released weights, no accompanying publication.
\end{thebibliography}}

\end{document}
