# --- data & environment (huge / machine-specific, never commit) ---
data/
env/
features_cache/

# --- regenerable artifacts that also embed absolute local paths ---
manifest.csv
manifest_balanced.csv
splits/
folds/

# --- verbose training logs ---
train_log.txt
train_log2.txt
cross_log.txt
cross_smoke.txt

# --- python / jupyter cruft ---
__pycache__/
*.pyc
.ipynb_checkpoints/
Untitled.ipynb

# --- editor / tooling ---
.claude/

# --- LaTeX build output (if you compile locally) ---
*.aux
*.log
*.out
*.synctex.gz
*.fls
*.fdb_latexmk
*.xdv
output.pdf
main.pdf
main_fa.pdf

# temp sample lists (contain absolute paths)
explanations/_*.csv

# --- model checkpoints (large / regenerable; some exceed GitHub's 100MB limit) ---
*.pt

# --- generated protocol manifests + checkpoints (path-bearing, regenerable) ---
protocols/

# --- full 5-corpus manifest (path-bearing, >100MB, regenerable) ---
manifest_all.csv

# --- verbose run logs ---
protoA_log.txt
download_log.txt

# --- local scratch workspace unzipped from the cloud run bundle (large ckpts) ---
cloud_bundle/

# --- adaptive-TTA smoke-run artifacts (throwaway dry-run outputs) ---
results_adaptive_smoke.csv
adaptive_trace_smoke.csv
run_log_adaptive_smoke.txt

# --- public third-party checkpoints (9+ GB, re-downloadable from HF) ---
public_ckpt/
# auto-generated by vendor_aasist.py
aasist_backend.py
# regenerable via build_itw_manifest.py, embeds absolute paths
manifest_itw.csv
run_log_public_ckpt.txt
run_log_cpu_smoke.txt
results_cpu_smoke.csv
public_ckpt_run.out
cpu_smoke.out
followup.out
seed2.out
results_public_ckpt.csv.bak
# --- autonomous run 2026-08-27..31 ---
# Target pools: path-bearing and regenerable via make_target_manifests.py, which
# size-gates every pool against results_ext.csv. Same reasoning as manifest.csv
# and manifest_itw.csv above (39 MB across 71 files).
manifest_tgt_*.csv
# Run state and per-job logs for the orchestrator; the digest is queue_master.log
# (covered by *.log) and the results CSVs.
queue_state.json
queue_log.txt
run_log_*.txt

# --- local LaTeX bibliography build output ---
*.bbl
*.blg
build_jasmp/

results_protocol_a_smoke.csv
